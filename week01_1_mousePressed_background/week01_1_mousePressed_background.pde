//桌面Processing資料夾，啟動
//File-Preference 字型放大
void setup(){ //setting window big or low
  size(500, 500);
}  
void draw(){
  // mouse reaction
  if(mousePressed) background(#FF0000);
  else background(#00FF00);
} //按三角形執行run
//File-SaveAs 存在桌面上
